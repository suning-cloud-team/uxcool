import Slider from './slider';
import Range from './range';
import Handle from './handle';

Slider.Range = Range;
Slider.Handle = Handle;

export { Slider as VSlider, Range as VSliderRange, Handle as VSliderHandle };
export default Slider;
